<%= form_for(category) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>

    <%= f.label :name, 'Имя' %>
    <%= f.text_field :name, class: 'form-control' %>

    <%= f.label :ctype, 'Тип' %>
    <%= f.select :ctype, Category.ctypes.keys, {}, {class: 'form-control'} %>

    <%= f.submit 'Добавить', class: 'btn btn-primary', id: 'submit_add_category' %>
<% end %>